[PROMPT: receipt_match_v1]
[VERSION: 1.0]
[DESCRIPTION: Basic receipt-to-transaction matching]

Match this receipt to a transaction:

Receipt:
- Merchant: {receipt_merchant}
- Amount: {receipt_amount} {receipt_currency}
- Date: {receipt_date}
- Category: {receipt_category}

Candidate Transaction:
- Merchant: {txn_merchant}
- Amount: {txn_amount} {txn_currency}
- Date: {txn_date}
- Category: {txn_category}

Matching tolerance:
- Amount: within {amount_tolerance_pct}%
- Date: within {date_tolerance_days} days

Determine if this is a match. If confidence < {low_confidence_threshold}, explain the discrepancy and suggest next steps.

Output JSON:
{
  "action": "auto_match" | "request_review",
  "message": "<explanation>",
  "confidence": <0.0-1.0>,
  "details": {
    "amount_diff": <difference>,
    "amount_diff_pct": <percentage>,
    "date_diff_days": <days>,
    "merchant_similarity": <0.0-1.0>,
    "possible_reasons": ["<reason1>"]
  }
}
